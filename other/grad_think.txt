Create 2 tensor instances:

    auto a = tensor::Tensor({1,2},{3,4});
    auto b = tensor::Tensor({5,6},{7,8});

Activate the grad:

    a.grad_active();
    b.grad_active();

    This operation needs to:
        - active requires_grad flag
        - initialize the empty grad tensor (where to store the result)
        - Activate all the other stuff? Maybe using std::optional
        - ...

Compute an operation.

    auto c = tensor::sum(a, b);

    This needs to:
        - create a new tensor with correct result values,
          requires_grad activated, parents node (?)

        <?> Parents need to be copies?
            Is there a way that does not involve copying for efficiency?
        
        Version counter: each tensor has a version number
                         that counts the in-place modifications.
                         This is used during the backprop: if the version 
                         is not correct the backprop fails.
        The version that has been used for the forward operation must be
        the same during the backpropagation.
        
        Problem with inplace operations: inplace change the version counter
        but they can be useful (avoid copying tensors in favour of shared_ptr to them)


        Each NODE (HOW/WHEN DO I DEFINE IT? WHEN THE OPERATION IS CALLED?)
        stores shared_ptrs to the parent nodes.

        Tensors stored only if required for computing the derivative.


Inline (?) operations:

    tensor::Tensor a({0,1},{2,3});
    tensor::Tensor b({0,1},{2,3});
    a += b;

    This must increment the version of the tensor. (Avoid problem with backpropagation)
